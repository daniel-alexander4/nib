--- a/internal/pdfops/pdfops.go
+++ b/internal/pdfops/pdfops.go
@@ -1189,13 +1189,7 @@
 // and wrong.
 func stampWidth(text, fontName string, pts int, embedded bool) float64 {
 	drawn := drawnFace(fontName, embedded)
-	widest := 0.0
-	for _, line := range strings.Split(text, "\n") {
-		if w := mdpdf.Width(line, drawn, pts, embedded); w > widest {
-			widest = w
-		}
-	}
-	return widest
+	return mdpdf.Width(text, drawn, pts, embedded)
 }
 
 // Fit is the measured relationship between one stamped field's text and the
