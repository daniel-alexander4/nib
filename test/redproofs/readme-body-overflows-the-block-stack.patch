diff --git a/internal/p2p/readme.go b/internal/p2p/readme.go
index 870058a..e9e1a95 100644
--- a/internal/p2p/readme.go
+++ b/internal/p2p/readme.go
@@ -55,6 +55,7 @@ var trustClaims = []string{
 const readmeTitle = "About this co-signed document"
 
 var readmeParagraphs = []string{
+	"Padding words that fill up a whole rendered line of the page. Padding words that fill up a whole rendered line of the page. Padding words that fill up a whole rendered line of the page. Padding words that fill up a whole rendered line of the page. Padding words that fill up a whole rendered line of the page. Padding words that fill up a whole rendered line of the page. Padding words that fill up a whole rendered line of the page. Padding words that fill up a whole rendered line of the page. Padding words that fill up a whole rendered line of the page. Padding words that fill up a whole rendered line of the page. Padding words that fill up a whole rendered line of the page. Padding words that fill up a whole rendered line of the page. ",
 	"This PDF was signed by two or more people using Nib, a tool that fills, signs, and verifies " +
 		"PDFs entirely on each person's own computer — nothing is uploaded. Each signature is an " +
 		"approval signature: it does not lock the document, but it proves that the content that " +
