--- a/web/index.html
+++ b/web/index.html
@@ -144,6 +144,12 @@
       <button id="reloadBtn" class="tbicon" title="Reload from disk, discarding unsaved changes" aria-label="Reload from disk" disabled>
         <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12a9 9 0 1 1-2.64-6.36"/><path d="M21 3v6h-6"/></svg>
       </button>
+            </div><div class="tbgroup" data-label="Close Document" data-fold="4">
+      <button id="closeBtn" title="Close this document and return to the empty state (Nib keeps running)" disabled>Close</button>
+      <!-- Appears at two open documents, matching the tab strip's own rule, because with
+      one document open Close view and Close all are the same act and offering both
+      would be two buttons for one outcome. syncTabs toggles it. -->
+      <button id="closeAllBtn" title="Close every open document and return to the empty state" hidden>Close all</button>
             </div><div class="tbgroup" data-label="Save" data-fold="0">
 <button id="saveBtn" title="Save (overwrites the original)" disabled>Save</button>
             </div>    </div>
@@ -229,13 +235,7 @@
       <button id="exportCertBtn">Signing certificate</button>
       <button id="printBtn" title="Print (opens your browser's print dialog)" disabled>Print…</button>
             </div>
-      <div class="tbgroup" data-label="Close Document" data-fold="4">
-      <button id="closeBtn" title="Close this document and return to the empty state (Nib keeps running)" disabled>Close</button>
-      <!-- Appears at two open documents, matching the tab strip's own rule, because with
-      one document open Close view and Close all are the same act and offering both
-      would be two buttons for one outcome. syncTabs toggles it. -->
-      <button id="closeAllBtn" title="Close every open document and return to the empty state" hidden>Close all</button>
-            </div>
+      
     </div><div class="tbtab" data-tab="markup">
 <div class="tbgroup" data-label="Detect &amp; Fill Fields" data-fold="0">
 <button id="detectBtn" title="Detect blank lines to fill">Detect fields</button>
