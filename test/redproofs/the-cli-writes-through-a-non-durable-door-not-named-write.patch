--- a/internal/cli/commands.go
+++ b/internal/cli/commands.go
@@ -1,6 +1,7 @@
 package cli
 
 import (
+	"nib/internal/atomicfile"
 	"context"
 	"crypto/sha256"
 	"encoding/json"
@@ -509,7 +510,7 @@
 		return 1
 	}
 	for i, p := range parts {
-		if err := writeNamedMode(outs[i], p.Data, pdfops.SplitPartMode); err != nil {
+		if _, err := atomicfile.WriteFrom(outs[i], nil, pdfops.SplitPartMode, int64(len(p.Data)), nil); err != nil {
 			errf("%v", err)
 			return 1
 		}
