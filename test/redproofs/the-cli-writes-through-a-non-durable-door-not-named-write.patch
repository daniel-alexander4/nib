--- a/internal/cli/commands.go
+++ b/internal/cli/commands.go
@@ -1,6 +1,7 @@
 package cli
 
 import (
+	"nib/internal/atomicfile"
 	"context"
 	"crypto/sha256"
 	"encoding/json"
@@ -484,7 +485,7 @@
 			errf("unsafe file name %q", p.Name)
 			return 1
 		}
-		if err := writeNamed(full, p.Data); err != nil {
+		if _, err := atomicfile.WriteFrom(full, nil, 0o644, 0, nil); err != nil {
 			errf("%v", err)
 			return 1
 		}
