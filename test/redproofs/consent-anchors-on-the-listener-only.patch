--- a/internal/server/session.go
+++ b/internal/server/session.go
@@ -557,12 +557,6 @@
 		if s == nil {
 			continue
 		}
-		if a.cer != nil {
-			if s.cer == a.cer {
-				return true
-			}
-			continue
-		}
 		if s.ln != nil && s.ln == a.ln {
 			return true
 		}
