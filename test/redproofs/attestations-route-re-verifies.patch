diff --git a/internal/server/cosign.go b/internal/server/cosign.go
index 95879ef..301fe49 100644
--- a/internal/server/cosign.go
+++ b/internal/server/cosign.go
@@ -142,7 +142,8 @@ func (s *Server) handleAttestations(w http.ResponseWriter, r *http.Request) {
 	if p2p.ClaimsAProceeding(doc.sig) {
 		proc = ceremony.ProceedingOf(s.docBytes(doc), time.Now())
 	}
-	atts := p2p.Attestations(doc.sig, proc)
+	_ = proc
+	atts := p2p.ReadAttestations(s.docBytes(doc))
 	views := make([]attestationView, 0, len(atts))
 	for _, a := range atts {
 		view := attestationView{SignerAttestation: a}
