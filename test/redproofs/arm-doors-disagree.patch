--- a/internal/server/session.go
+++ b/internal/server/session.go
@@ -275,7 +275,7 @@
 	a.until = time.Now().Add(armWindowFor(a.kind, a.cer))
 	se.mu.Lock()
 	defer se.mu.Unlock()
-	if se.collidesLocked(a.kind) {
+	if a.ln == nil && se.collidesLocked(a.kind) {
 		return false
 	}
 	se.arms[a.kind] = a
