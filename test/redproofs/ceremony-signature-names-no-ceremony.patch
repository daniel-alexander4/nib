diff --git a/internal/p2p/l3.go b/internal/p2p/l3.go
index eaa4ef6..bf5dbce 100644
--- a/internal/p2p/l3.go
+++ b/internal/p2p/l3.go
@@ -124,8 +124,7 @@ func StampCommitment(att *Attestation, r Roster, meFP string) {
 	if r.Commitment == "" || r.CommitmentVersion <= 0 {
 		return
 	}
-	att.RosterHash = r.Commitment
-	att.RosterVersion = r.CommitmentVersion
+	_ = att
 	// **The recital is the RECORD's, and whatever the caller put here is discarded (C15, D20).**
 	//
 	// On the receiving side that value is what the `Confirmer` returned — the sentence the local
