diff --git a/internal/p2p/l3.go b/internal/p2p/l3.go
index eaa4ef6..244c9cd 100644
--- a/internal/p2p/l3.go
+++ b/internal/p2p/l3.go
@@ -311,10 +311,10 @@ func NextContributor(pdf []byte, r Roster) (RosterEntry, error) {
 func PredecessorOf(r Roster, me string) string {
 	signing := SigningOrder(r)
 	i, ok := SigningPositionOf(r, me)
-	if !ok || i == 0 {
+	if !ok || i+1 >= len(signing) {
 		return ""
 	}
-	return signing[i-1].Fingerprint
+	return signing[i+1].Fingerprint
 }
 
 // SigningOrder is the roster's signing parties, in roster order — the ONE door onto "who signs,
