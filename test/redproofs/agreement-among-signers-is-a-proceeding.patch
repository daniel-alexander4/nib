diff --git a/internal/p2p/attestation.go b/internal/p2p/attestation.go
index ff6335d..78d962a 100644
--- a/internal/p2p/attestation.go
+++ b/internal/p2p/attestation.go
@@ -352,16 +352,21 @@ func shortFingerprint(hexFP string) string {
 // An empty `want` therefore disqualifies: a caller that cannot say which ceremony this document
 // belongs to must not be able to produce the ✓.
 func markOneProceeding(ats []SignerAttestation, want string) {
-	if want == "" {
-		return
-	}
+	first := ""
 	n := 0
 	for _, a := range ats {
 		if !a.Valid {
 			continue
 		}
 		n++
-		if !strings.EqualFold(a.RosterHash, want) {
+		if a.RosterHash == "" {
+			return
+		}
+		if first == "" {
+			first = a.RosterHash
+			continue
+		}
+		if a.RosterHash != first {
 			return
 		}
 	}
