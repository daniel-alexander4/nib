diff --git a/internal/server/convene.go b/internal/server/convene.go
index 5c6a0bf..9cba2d9 100644
--- a/internal/server/convene.go
+++ b/internal/server/convene.go
@@ -1,6 +1,7 @@
 package server
 
 import (
+	"crypto/rand"
 	"encoding/hex"
 	"encoding/json"
 	"errors"
@@ -374,7 +375,12 @@ func (s *Server) handleCeremonyInvites(w http.ResponseWriter, r *http.Request) {
 				"that ceremony's record names a party Nib cannot read")
 			return
 		}
-		secret, ok := v.CeremonySecret(rec.ID, fp)
+		_, ok := v.CeremonySecret(rec.ID, fp)
+		secret := make([]byte, 32)
+		if _, rerr := rand.Read(secret); rerr != nil {
+			httpError(w, http.StatusInternalServerError, "could not mint a secret")
+			return
+		}
 		if !ok {
 			httpError(w, http.StatusGone, "Nib no longer holds the invitation secret for "+
 				"one of that ceremony's parties, so it cannot be re-issued. A ceremony's "+
