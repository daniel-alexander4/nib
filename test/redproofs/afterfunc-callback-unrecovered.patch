diff --git a/internal/server/session.go b/internal/server/session.go
index 1fd2157..395bce5 100644
--- a/internal/server/session.go
+++ b/internal/server/session.go
@@ -695,10 +695,7 @@ func (s *Server) runSession(ln p2p.Listener, cer *ceremonyID, cert, key []byte,
 	// the co-signed document opening ONCE across re-deliveries. Both mirror runCeremonyReceive.
 	var postSign time.Time
 	opened := false
-	timer := time.AfterFunc(sessionAcceptTimeout, func() {
-		defer safe.Recover("arm window expiry")
-		s.sess.disarmIf(ln)
-	})
+	timer := time.AfterFunc(sessionAcceptTimeout, func() { s.sess.disarmIf(ln) })
 	defer timer.Stop()
 	defer s.sess.disarmIf(ln)
 
