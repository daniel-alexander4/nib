diff --git a/internal/ceremony/mirror.go b/internal/ceremony/mirror.go
index 810eec4..6e15e31 100644
--- a/internal/ceremony/mirror.go
+++ b/internal/ceremony/mirror.go
@@ -436,7 +436,6 @@ func ReadStored(root, id string, now time.Time) Stored {
 	// reader most wants it for, a party who has accepted and not yet signed, is precisely one of
 	// the branches that returns early. Placing it after them would have made it invisible for
 	// every ceremony whose document has not arrived.
-	s.Verification = readVerification(dir)
 	s.Joined = readMe(dir) != ""
 	b, err := os.ReadFile(filepath.Join(dir, "record.json"))
 	if err != nil {
@@ -489,6 +488,7 @@ func ReadStored(root, id string, now time.Time) Stored {
 			"damaged"
 		return s
 	}
+	s.Verification = readVerification(dir)
 	if verr := r.Verify(now); verr != nil {
 		// **Skew first, and it is the ordering that matters.** `Verify` checks the version before
 		// anything else, so every skewed record also fails the checks below it; classifying in the
