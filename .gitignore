# build output
/nib
/nib.exe
/dist/
*.test

# local verification / scratch artifacts
.playwright-mcp/
tmpgen/
tshoot/
overlay-*.png

# project instructions stay local (Dan's convention, 2026-07-09)
/CLAUDE.md

# OS cruft
.DS_Store

# dev-only front-end test harness (package.json is checked in; its deps are not)
/node_modules/

# Claude Code local context — never committed
.claude/
CLAUDE.md
